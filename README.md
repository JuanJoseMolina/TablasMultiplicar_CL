# Notas:

Aplicacion de consola de NODE que crea tablas de multiplicar
```
Opciones:
      --help      Muestra ayuda                                       [booleano]
      --version   Muestra número de versión                           [booleano]
    -b, --base      introducir la base de la tabla
                                                [número] [requerido] [defecto: 5]
  -l, --longitud  longitud de la tabla de mutiplicar      [número] [defecto: 10]
  -c, --vconsola  True -> crea la lista en la consola[booleano] [defecto: false]
```